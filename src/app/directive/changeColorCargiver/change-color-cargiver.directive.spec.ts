import { ChangeColorCargiverDirective } from './change-color-cargiver.directive';

describe('ChangeColorCargiverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorCargiverDirective();
    expect(directive).toBeTruthy();
  });
});
